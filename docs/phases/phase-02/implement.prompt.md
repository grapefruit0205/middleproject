# Phase 02 DeepSeek Implementation Prompt

기준 commit: `<BASE_COMMIT>`

`project-invariants.md`와 Phase 02 brief를 읽어라. `backend/`와 Phase 결과 문서만 수정하라. Event, Reminder, NotificationPolicy를 최소 모델로 구현하고 상태 전이, Validation, Optimistic Lock, Idempotency를 테스트하라.

Controller와 Domain 사이에 Application Service를 둬라. MCP나 Scheduler 코드는 만들지 마라. Migration과 테스트를 실행하고 명령·Exit Code·제약을 `result.md`에 기록하라.
