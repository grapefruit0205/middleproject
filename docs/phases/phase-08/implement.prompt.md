# Phase 08 DeepSeek Implementation Prompt

기준 commit: `<BASE_COMMIT>`

`project-invariants.md`와 Phase 08 brief를 읽어라. Failure Matrix의 각 시나리오를 재현하는 테스트를 먼저 작성하고 최소 구현으로 통과시켜라. Idempotency Record, Version, Attempt를 사용하되 새 분산 시스템 제품을 추가하지 마라.

Exactly-once라는 표현을 사용하지 마라. 중복을 감지하고 결과를 재사용하는 범위를 문서화하라. 테스트 명령과 결과를 `result.md`에 기록하라.
