# Phase 09 DeepSeek Implementation Prompt

기준 commit: `<BASE_COMMIT>`

`project-invariants.md`와 Phase 09 brief를 읽어라. MCP Adapter가 기존 Application Service를 호출하도록 구현하라. Tool Schema, 사용자 Identity, Authorization, Idempotency를 검증하라.

SQL, Shell, SSH, Direct DB, Arbitrary HTTP Tool을 만들지 마라. MCP 전용 Domain Service를 복제하지 마라. Contract와 Security Test 결과를 `result.md`에 기록하라.
