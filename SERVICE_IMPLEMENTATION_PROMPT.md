# middleproject 서비스 완성 — 연속 구현 프롬프트

작성일: 2026-09-07 · 진행 원본: `progress.json` · 구현 트랙: `service-mvp`

## 사용 방법

저장소 루트에서 실행하는 코딩 에이전트에게 다음과 같이 요청한다.

> SERVICE_IMPLEMENTATION_PROMPT.md와 progress.json을 읽고 서비스 구현을 시작해줘. 완료한 Phase는 재작업하지 말고, 외부 승인이나 필수 정보가 필요하지 않으면 다음 Phase까지 계속 구현해줘. 세션이 중단되었다면 progress.json의 resume에서 이어서 진행해줘.

**이 문서와 JSON을 작성·검토해 달라는 요청만으로 서비스 구현을 시작하지 않는다.** 위와 같은 실제 시작·재개 요청이 있어야 실행한다. `progress.json`은 에이전트의 진행 기록이지 스케줄러나 실행 프로그램이 아니다. 종료된 세션을 스스로 깨우거나 새 작업을 생성하지 않는다.

---

## 1. 목표와 범위

너는 현재 `middleproject`의 Java/Spring Boot 백엔드와 React 프론트엔드를 활용해 **일정·마감 알림 웹서비스**를 완성하는 구현 에이전트다. 계획서 추가 생산이 아니라 동작하는 코드를 만든다.

사용자는 자격증 접수, 예매 시작, 서류 제출처럼 놓치기 쉬운 일정을 등록한다. 사이트를 닫아도 서버가 알림을 처리하고, 다시 접속하면 일정과 처리 이력을 확인할 수 있어야 한다.

이번 작업의 기본 범위는 **본인용 단일 소유자 데모, 일정당 이메일 알림 1개, Asia/Seoul 기준**이다(`assumed`). 다중 사용자 SaaS, 여러 번의 반복 알림, 자동 수집까지 확대하지 않는다. 이 기본값은 `memory/OPEN-QUESTIONS.md`에 있으며 사용자가 수정하면 변경 범위를 반영한다.

완성할 사용자 흐름:

1. 제목·일정 시각·알림 선행 시간을 입력하고 등록한다.
2. 내 대시보드에서 임박한 일정, 다음 알림 시각, 예약 상태를 확인한다.
3. 일정을 수정하거나 취소하고, 새로고침해도 변경 사항이 유지된다.
4. 화면을 닫아도 서버 측 예약·발송이 진행된다.
5. 알림 처리 이력과 실제 이메일 수신을 확인한다.

기본 날짜 입력 폼이 필수다. 기존 규칙 기반 자연어 파싱은 해석 결과 미리보기와 확인을 거쳐 사용할 수 있지만, 모호하면 날짜를 직접 입력하게 한다. 자유로운 AI 대화가 완성의 선행 조건은 아니다.

## 2. 시작할 때 한 번만 확인할 것

1. `AGENTS.md`, `memory/00-INDEX.md`, `memory/DECISIONS.md`, 이 문서, `progress.json`을 읽는다.
2. 현재 경로·Git 상태·변경 파일을 확인한다. 사용자 변경을 보존하고 다른 저장소를 수정하지 않는다.
3. `memory/PRODUCT-TRUTH.md`와 현재 Phase에 필요한 코드만 읽는다. 소스 존재, 실제 연결, 실행 확인을 구분한다.
4. 기존 `docs/phases/phase-00`~`phase-18`은 과거 작업 기록이다. 이번 S01~S05와 합산하거나 번호를 바꾸지 않는다. MyWiki 및 출장 코파일럿 구현을 재개하지 않는다.
5. 실제 실행 도구는 `frontend/package.json`, Gradle Wrapper, 기존 배포 파일에서 확인한다. 없는 npm 스크립트나 실행 가능한 Spring Boot JAR를 가정하지 않는다.

이번 트랙은 사용자의 경량 검증 요청에 따라 **단계별 별도 리뷰·PASS 승인·복수 에이전트 검토를 기본 진행 조건으로 사용하지 않는다.** 과거 문서의 DeepSeek 구현 → Codex 독립 리뷰 루프를 자동 실행하지 않는다. 다음 기술·보안 불변 조건은 유지한다.

## 3. 유지할 아키텍처와 구현 원칙

- AWS 경로: Public ALB → private Apache WEB → internal ALB → private external Tomcat WAS → RDS PostgreSQL.
- 서울 리전, 2개 AZ의 네트워크, WEB/WAS/DB 서브넷·보안 그룹 분리, SSM 관리 접근을 유지한다. Bastion·SSH·공인 IP를 추가하지 않는다.
- Java 21·Spring Boot·외장 Tomcat용 `ROOT.war`·React를 유지한다. 마이크로서비스나 Kubernetes로 전환하지 않는다.
- PostgreSQL이 업무 상태의 원본이다. 사용자 상태를 브라우저 localStorage나 WAS 인스턴스 메모리에만 저장하지 않는다.
- 기존 Event/Policy/Reminder, Idempotency, 낙관적 잠금, Outbox, Scheduler, SQS, NotificationSender를 재사용한다. 기존 코드가 있다는 이유만으로 새 사용자 흐름의 완료를 선언하지 않는다.
- 필요한 사용자 흐름용 Application Service와 조회 API는 추가할 수 있다. 컨트롤러나 화면에 업무 규칙을 복제하지 않는다.
- 스키마 변경은 새 Flyway 마이그레이션을 추가한다. 적용 이력이 있는 V1~V6 등을 덮어쓰거나 DB를 초기화하지 않는다.
- DB 저장 성공과 Scheduler 예약 성공을 구분한다. Outbox에 기록하고 응답하며, 이메일 발송 완료까지 웹 요청을 붙잡지 않는다.
- 브라우저가 여러 CRUD API를 호출하다 중간에 실패하는 방식보다, 등록 단위를 일관되게 저장하는 서버 측 트랜잭션을 사용한다.
- 이메일 전송 제공자의 요청 수락을 수신함 도착·사용자 열람으로 표시하지 않는다. 타임아웃으로 결과가 불명확한 상태도 구분하고 무조건 재발송하지 않는다.
- 프론트엔드에 AWS 자격증명이나 서버 비밀을 넣지 않는다. 민감한 원문·이메일·토큰을 진행 파일이나 로그에 남기지 않는다.
- `ReminderCommandParser` 경계를 유지한다. Bedrock/SageMaker 어댑터, 학습, RAG, 모델 서버는 이번 범위에서 구현하지 않는다.

## 4. 연속 진행 규칙

### 실행 루프

1. `progress.json`의 미완료 Phase를 의존성 순서로 선택한다. `in_progress`가 있으면 먼저 재개한다. `resume.phase_id`와 실제 상태가 다르면 근거를 확인해 포인터만 바로잡는다.
2. 선택한 Phase를 `in_progress`로, 전체 상태를 `running`으로 바꾼다. 동시에 진행 중인 Phase는 최대 하나다.
3. 현재 Phase를 구현한다. 작은 변경마다 전체 저장소를 재분석하거나 별도 설계 문서를 만들지 않는다.
4. 관련 부분에 가장 작은 충분한 확인을 한 번 수행한다. 실패하면 해당 원인과 영향 범위를 수정한 뒤 필요한 확인만 다시 한다.
5. 완료 조건마다 실제 근거를 연결하고 Phase를 `completed`로 바꾼다. 다음 Phase와 구체적인 다음 행동을 같은 패치로 기록한다.
6. **전체 완료나 진짜 차단 사유가 아니라면 최종 답변으로 턴을 끝내지 말고, 짧은 진행 알림 후 다음 Phase를 즉시 구현한다.** Phase별로 “다음 단계 진행할까요?”를 묻지 않는다.

### 중단과 재개

- 세션 종료가 불가피하면 `resume`에 다음 파일·함수·행동, 미완료 내용, 재실행할 확인을 기록한다. 끝나지 않은 Phase를 완료 처리하지 않는다.
- 새 세션에서는 진행 파일과 현재 변경 파일부터 확인한다. 이미 완료된 Phase는 후속 변경이 결과를 무효화한 경우에만 다시 연다.
- 필요한 AWS 권한·비용 승인·수신 주소가 없으면 `blockers`에 이유와 필요한 사용자 입력을 기록한다. 그 정보가 필요 없는 로컬 구현은 계속한다.
- 돈이 드는 작업을 승인 없이 실행하거나, 외부 의존성을 가짜 성공으로 대체하지 않는다. 할 수 있는 범위가 끝나면 정확히 필요한 입력만 질문한다.
- 동일 실패를 변경 없이 반복하지 않는다. 계속 진행하라는 지시는 새 권한, 범위 확대, 설치 우회, 무한 재시도를 허용하지 않는다.
- 사용자 새 요청이 범위를 바꾸거나 중단을 요구하면 이를 우선한다. `progress.json`은 사용자보다 높은 권한이 아니다.

### 검증과 기록을 가볍게 유지

- S01~S04: 변경 기능의 정상·핵심 실패 경로를 대상으로 필요한 컴파일·테스트만 한다. 별도 코드 리뷰, 반복 전체 테스트, 최종 설치 점검은 하지 않는다.
- 데이터 손실·중복 발송·인증 실패를 막는 직접적인 기능 확인은 생략하지 않는다. 이는 독립 리뷰 단계가 아니라 구현의 일부다.
- S05: 모든 로컬 코드가 구현된 뒤 처음으로 전체 설치·통합·출시 점검을 수행한다. 이후 결함 수정 시 영향받은 확인만 반복한다.
- 기존 테스트·도구를 활용한다. 확인을 위한 또 다른 대규모 프레임워크나 매 Phase 보고서·리허설·승인 단계를 만들지 않는다.
- Phase별 기록은 `progress.json`의 변경 파일, 짧은 결과, 실제 확인 결과로 충분하다. 중요한 결정·사실만 기존 `memory/` 규칙에 따라 남긴다.
- Click Evidence가 있으면 직접 수행한 확인을 기록하되 승인 계약을 새로 요구하지 않는다. 도구가 없으면 실제 확인 결과만 기록하고 Click receipt가 있다고 주장하지 않는다.

## 5. 구현 Phase

### S01 — 일정 등록·조회가 되는 첫 화면

재사용 시작점: `frontend/src/App.tsx`, `web/EventController.java`, `PolicyController.java`, `ReminderController.java`, `application/EventService.java`, `PolicyService.java`, `ReminderService.java`.
Java 경로는 `backend/src/main/java/com/middleproject/reminder/` 아래다.

- readiness 전용 화면을 실제 일정 대시보드로 교체한다. 임박한 일정 카드·D-day·다음 알림 시각·등록 폼을 제공한다.
- 일정 등록 한 번에 Event/Policy/Reminder와 필요한 Outbox를 일관되게 생성하는 사용자 흐름을 연결한다. 멱등키는 논리적 제출 단위로 재사용한다.
- 목록·상세 조회가 필요한 정보를 제공하도록 기존 API를 연결하거나 좁은 조합 조회 API를 추가한다.
- 빈 목록, 로딩, 입력 오류, API 실패를 처리한다. 모바일 너비에서도 등록과 조회가 가능해야 한다.
- 개발 실행은 기본적으로 loopback/로컬에 한정한다. 보호되지 않은 개발 API를 인터넷에 공개하지 않는다.

완료 조건:

- `S01-A1`: 화면에서 등록한 일정과 알림 설정이 PostgreSQL에 저장되고 새로고침 후 동일하게 조회된다.
- `S01-A2`: 필수 값 누락·잘못된 시각을 차단하고, 요청 실패 시 성공 카드나 부분 저장 잔재가 생기지 않는다.
- `S01-A3`: API 실패·빈 목록·로딩을 실제 상태로 표시하며, 좁은 화면에서도 핵심 폼과 목록을 사용할 수 있다.

### S02 — 수정·취소와 중복·충돌 처리

- 일정 상세에서 시각·제목·알림 선행 시간을 수정하고 취소할 수 있게 한다. 취소는 상태와 이력을 남기며 파괴적 전체 삭제로 구현하지 않는다.
- 날짜를 바꾸면 연결된 Reminder/Outbox/Scheduler 세대도 함께 갱신되게 한다. Event만 바꾸고 이전 알림을 남기는 경로를 막는다.
- 저장 버튼 중복 클릭과 같은 멱등키 요청 재전송을 처리한다. 버전 충돌 시 무조건 덮어쓰지 말고 최신 내용 확인을 안내한다.
- 취소·변경 전 메시지가 늦게 도착해도 현재 버전·상태와 비교해 처리한다. 이미 제공자에 접수된 메일을 취소할 수 있다고 약속하지 않는다.
- 기존 자연어 `/api/reminder-commands/parse`를 사용할 경우 미리보기 → 사용자 확인 → 동일 등록 서비스로 연결한다. 해석 실패나 모호함은 직접 입력으로 복귀한다.

완료 조건:

- `S02-A1`: 시각·선행 시간 변경이 알림 예약 변경까지 이어지고, 새로고침 후에도 같은 결과를 보여준다.
- `S02-A2`: 취소 상태가 유지되며, 통제된 이전 버전 메시지는 추가 발송 처리로 진행되지 않는다.
- `S02-A3`: 동일 요청 재전송은 중복 일정을 만들지 않고, 오래된 버전 수정은 충돌로 처리한다.

### S03 — 서버 측 알림 처리와 이력 화면

- 기존 Scheduler/Outbox/SQS/SES 어댑터와 worker 설정을 실제 서비스 흐름에 연결한다. 브라우저 타이머로 발송하지 않는다.
- 알림별 예약 상태·발송 시도·실패 원인·확인 가능한 결과를 조회하는 API와 상세 타임라인을 만든다. 내부 오류의 비밀값은 숨긴다.
- 단일 소유자의 수신 주소는 서버 설정으로 주입한다. 임의 주소 대량 발송 기능을 만들지 않는다.
- 발송 기능이 비활성화됐거나 NoOp/테스트 제공자이면 화면에도 모의/비활성 상태로 표시한다. 외부 연동이 없는 상태를 발송 성공으로 가장하지 않는다.
- 로컬 targeted 확인에는 기존 fake/테스트 어댑터를 사용할 수 있다. 실제 SES 수신 증거는 S05에서 따로 확보하며, fake 결과로 대체하지 않는다.
- 실패·재처리 정책은 기존 엔진을 활용하고 결과 불명확 상태와 확실한 실패를 구분한다. 무한 자동 재시도나 UI의 강제 재발송 버튼을 추가하지 않는다.

완료 조건:

- `S03-A1`: 일정 저장 요청이 발송 완료를 기다리지 않으며 worker가 서버 측에서 처리하도록 연결되어 있다.
- `S03-A2`: 이력 API와 화면이 영속화된 상태·시도 결과를 표시하고, 성공·실패·비활성 상태를 구분한다.
- `S03-A3`: 통제된 중복 전달과 일시 실패 경로에서 기존 중복 방지·복구 정책이 작동한다. 외부 수신 보장은 별도로 남긴다.

### S04 — 제한된 데모를 위한 보안·배포 코드 마무리

- 웹 API도 서버가 확인한 단일 소유자 인증 경계 안에 둔다. 기존 ADR의 비공개 단일 사용자 방향을 유지하고 OAuth SaaS·Android 페어링으로 확대하지 않는다.
- 서버에서 검증할 수 있고 폐기 가능한 인증 수단을 사용한다. 사용자 입력 ownerId를 신뢰하거나 공유 비밀을 번들·localStorage에 넣지 않는다. 쿠키를 쓰면 CSRF·HttpOnly·Secure·SameSite를 고려하고 WAS 메모리 세션에 의존하지 않는다.
- 외부 실행 프로필은 인증 설정이 없으면 보호 API를 열지 않는다. 접근 제한은 화면 버튼뿐 아니라 등록·조회·수정·이력 API 전체에 적용한다.
- 공개 ALB에서 `/api/mcp`와 `/api/mcp/*`를 차단하는 규칙을 코드에 반영한다. 기존 비공개 MCP 경계를 공개하지 않는다.
- DB 관리자·마이그레이션·앱 실행 계정 및 IAM/시크릿 접근을 분리한다. 앱은 RDS 마스터 시크릿을 읽지 않는다. Flyway 계정과 실행 순서를 명시한다.
- 현재 Terraform 틀에서 WEB/WAS 수량·RDS Multi-AZ를 설정 가능하게 한다. HA 기본값은 2대/2대·Multi-AZ를 유지하고, 축소 실습 프로필은 고가용성 미보장임을 표시한다. 실제 부하 기반 확장은 이번 필수가 아니다.
- 최종 실행에 필요한 환경변수 예제·설치/실행 명령·짧은 데모 절차를 한 곳에 정리한다. 로컬 재현 경로가 없으면 PostgreSQL·외장 Tomcat·WEB으로 실행 가능한 최소 구성을 제공한다.
- **이 Phase에서는 배포 소스와 실행 경로를 완성한다. 최종 설치·새 환경 전체 시연·AWS apply는 아직 실행하지 않는다.**

완료 조건:

- `S04-A1`: 인증 없는 보호 API 접근이 거부되고, 소유자·버전·멱등 정책이 서버에서 적용된다.
- `S04-A2`: 공개 MCP 차단, 계층별 네트워크 경계, DB 실행 계정 최소 권한이 소스와 필요한 targeted 확인으로 뒷받침된다.
- `S04-A3`: 외장 Tomcat WAR 및 PostgreSQL 실행·마이그레이션·환경 설정이 재현 가능한 코드/명령으로 제공되고, 비밀값은 제외되어 있다.

### S05 — 마지막 설치·통합·실메일 시연

S01~S04가 완료된 뒤에만 시작한다. 기존 데이터는 보존하고 격리된 최종 확인 환경을 사용한다.

- 관련 전체 빌드·테스트와 설치/실행 흐름을 확인한다. PostgreSQL과 외장 Tomcat WAR를 포함하고, 테스트용 H2만으로 최종 DB 확인을 대신하지 않는다.
- 브라우저에서 등록 → 새로고침 → 수정 → 취소 → 이력 확인을 한 번의 시나리오로 확인한다. 중복 요청·충돌 등 핵심 경계도 포함한다.
- AWS 비용·계정·리전·배포 대상·유효시간·종료 계획의 명시적 승인을 확보한 뒤에만 배포한다. 승인되지 않은 apply/destroy, 새 계정 리소스, DNS 변경을 실행하지 않는다.
- 승인된 HTTPS WEB → WAS → RDS 경로에서 서비스와 접근 제한을 확인한다. 서버 장애 주입·RDS 강제 failover·부하 시험은 기본 필수가 아니다.
- 명시적으로 승인된 발신/수신 설정으로 가까운 미래에 이메일 알림을 예약한다. 페이지를 닫아도 처리되고 수신함에 도착하는지 확인한다. 제공자 접수 결과와 실제 수신 증거를 각각 남긴다.
- 결함은 수정하되 관련 확인만 재실행한다. 짧은 실행 방법·남은 제약·실제 확인 결과를 남기고 종료한다.

완료 조건:

- `S05-A1`: 최종 로컬 설치·빌드·통합 시나리오가 PostgreSQL/외장 Tomcat을 포함해 통과한다.
- `S05-A2`: 승인된 AWS 3-Tier 환경에서 HTTPS 사용자 흐름과 비인가 접근 차단을 직접 확인한다.
- `S05-A3`: 승인된 주소로 실제 예약 이메일 수신을 확인하고, 제공자 접수와 수신 증거를 구분한다.

AWS 승인이나 메일 설정이 없어 S05-A2/A3를 확인하지 못하면 **로컬 완료·외부 최종 확인 대기**라고 보고한다. 전체 `completed`로 바꾸지 않는다. 사용자가 완료 범위를 명시적으로 축소하면 그 결정을 기록한 뒤 범위를 수정하며, 실행하지 않은 AWS·이메일 항목을 성공으로 바꾸지 않는다.

## 6. progress.json 갱신 계약

- `schema_version`: 현재 1. 알 수 없는 버전이면 진행 파일을 초기화하지 말고 차이를 확인한다.
- 전체 `status`: `ready`, `running`, `blocked`, `completed` 중 하나. Phase `status`: `pending`, `in_progress`, `blocked`, `completed` 중 하나.
- `current_phase`: 현재 실행 중인 Phase ID, 없으면 null. `resume.phase_id`: 시작·재개할 Phase ID, 모두 완료하면 null.
- `depends_on`: 선행 Phase ID 목록. 선행 Phase가 완료되지 않으면 다음 Phase를 완료 처리하지 않는다.
- `acceptance[].satisfied`: 직접 확인된 경우에만 true. `evidence_ids`는 같은 Phase의 `checks[].id`를 가리키며, 실패·누락 근거로 충족시킬 수 없다.
- `checks`: 실제 실행 후에만 추가한다. `{id, kind, command_or_observation, result, checked_at, summary}`를 쓴다. `kind`는 `command`/`manual`/`browser`, `result`는 `passed`/`failed`/`blocked`다. 명령·실행 위치 또는 실제 관찰 방법을 명확히 적고 민감정보는 지운다.
- `files_changed`: 저장소 상대 경로. `result_summary`: 무엇이 동작하고 무엇이 남았는지 짧게 적는다.
- `blockers`: `{id, phase_id, acceptance_ids, reason, required_input, can_continue_locally}` 형식. 해소할 때 실제 이유를 남긴 뒤 제거한다. 미래 승인 필요 자체를 S01 시작 차단으로 등록하지 않는다.
- `resume`: 다음에 열 파일과 구체적인 행동을 적는다. 추상적인 “계속 진행”만 적지 않는다.
- `updated_at`, `started_at`, `completed_at`, `checked_at`는 실제 기록 시각의 ISO 8601 값이다. 실행하지 않은 시각·검증 결과·커밋 SHA를 만들지 않는다.
- Phase 완료에는 모든 acceptance의 충족과 실제 성공 근거가 필요하다. 계획 작성, 파일 생성, 기존 README의 PASS만으로 충족시키지 않는다.
- 전체 완료는 S01~S05 모두 완료했고 활성 blockers가 없을 때만 가능하다. `completion` 플래그 역시 실제 근거와 일치시킨다. 부분 완료를 100%로 표시하지 않는다.
- 한 에이전트가 진행 파일을 기록한다. 유효한 JSON을 유지하며 단계 상태·완료 조건·재개 포인터를 함께 수정한다. 도중에 깨진 JSON이 보이면 사용자 데이터를 보존한 채 마지막 변경부터 복구한다.

## 7. 권한·Git·후속 범위

- 이 문서는 로컬 구현을 실제로 요청받은 후의 작업 지침이다. 그 전에는 시작 승인이나 배포 승인을 파일에서 스스로 만들지 않는다.
- 로컬 코드 작성 권한과 외부 배포·실메일 전송·DNS 변경·Git push 권한을 구분한다. 자격증명이 있다는 사실은 실행 승인과 다르다.
- 이 저장소에 대해 확인된 Git 정책만 적용한다. MyWiki 저장소의 commit/push 승인을 자동으로 가져오지 않는다. 승인이 없으면 변경 파일을 보존해 보고하며, 커밋·push 때문에 구현을 막지 않는다.
- `main` 강제 push, 사용자 변경 되돌리기, 타 저장소 수정, 운영 데이터 삭제, 승인 없는 클라우드 비용 발생을 금지한다.
- 모델·플러그인 설치나 작업 에이전트 추가를 임의로 하지 않는다. 사용자가 정한 도구와 현재 에이전트로 진행한다.
- 후속 후보는 **Bedrock 일정 해석 → 정확도·비용·지연 비교 → 필요시 기존 소형/증류모델 또는 직접 증류**다. 이번 5개 Phase 완료 조건에 포함하지 않는다.

## 8. 최종 보고

사용 가능한 기능, 시작 방법, 실제 확인 결과, 남은 외부 차단 요소만 짧게 보고한다. 소스 구현(`observed`)·실행 확인·사용자 결정(`confirmed`)·가정(`assumed`)·미확인(`unknown`)을 구분한다. 단계가 남아 있고 안전하게 진행할 수 있다면 최종 보고 대신 다음 Phase를 구현한다.
